{
  "name": "notnote",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
