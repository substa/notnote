# Verify source behavior, generated browser artifacts, and both runtime container variants.
name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install build tooling
        run: npm ci
      - name: Verify production bundle
        run: npm run build:check
      - name: Python tests
        run: |
          python -m unittest discover -s tests -p 'test_*.py'
          python -m py_compile server.py
      - name: JavaScript tests
        run: npm test
      - name: Build default container
        run: docker build --file docker/Dockerfile --target runtime -t notnote:test .
      - name: Build Git-enabled container
        run: docker build --file docker/Dockerfile --target runtime-git -t notnote:test-git .
